package com.rendrapcx.tts.constant

enum class ColorAttr {
    COLOR_BACKGROUND,
    COLOR_ACTIVE,
    COLOR_DISABLE,
    COLOR_SELECTED,
    COLOR_RANGE_SELECT,
}

enum class TextAttr {
    CLEAR_TEXT,
    FILL_TEXT,
}

enum class InputDirection {
    ROW, COLUMN, BOTH, UNKNOWN
}
enum class PickBy{SPINNER, TOUCH, DIALOG}
enum class InputMode { NEW, EDIT }

enum class Direction { HORIZONTAL, VERTICAL }